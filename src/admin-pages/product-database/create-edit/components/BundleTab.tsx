"use client";
import BundleSection from "@/components/common/product/BundleSection";
import React from "react";

const BundleTab = ({ productId }: { productId: string }) => {
  return <BundleSection />;
};

export default BundleTab;
